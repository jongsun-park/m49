<div class="custom-product-banner__container" data-cc-animate>
  <h2 class="custom-product-banner__title">{{ collection.title }}</h2>
  <p class="custom-product-banner__description">{{ collection.description }}</p>
</div>

{% schema %}
  {
    "name": "Custom Product Banner",
    "class": "custom-product-banner",
    "settings": [],
    "presets": [
      {
        "name": "Custom Product Banner"
      }
    ]
  }
{% endschema %}